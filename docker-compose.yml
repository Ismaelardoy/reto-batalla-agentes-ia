services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: batalla_chatbots_api
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
      - ./tests:/app/tests
    env_file:
      - .env
    restart: unless-stopped
